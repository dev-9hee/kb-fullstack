package ch05.sec12;

// enum : 한정된 값으로 이루어짐, 요일과 계절처럼 한정된 값을 갖는 타입
public enum Week { /// 열거 타입 이름
    // 열거 상수 목록(한정된 값 목록)
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY,
}
